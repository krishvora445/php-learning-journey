import HomeCsontroller from './HomeCsontroller'
import Settings from './Settings'
const Controllers = {
    HomeCsontroller: Object.assign(HomeCsontroller, HomeCsontroller),
Settings: Object.assign(Settings, Settings),
}

export default Controllers